{
  "name": "kiralearn",
  "version": "1.0.0",
  "description": "Static site for kiralearn.space — generated from /kira-learn skill output",
  "private": true,
  "type": "module",
  "scripts": {
    "regenerate": "node build-index.js",
    "test": "node --test 'tests/*.test.js'"
  },
  "engines": {
    "node": ">=20"
  }
}
